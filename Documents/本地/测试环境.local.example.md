# 本机测试环境（模板）

> 这是提交到仓库的模板文件。真实文件是 `测试环境.local.md`（已被 `.gitignore` 忽略，不会提交），
> 复制本模板为 `测试环境.local.md` 后按自己机器的真实路径填写。

## 仓库

- 仓库路径：`<你的仓库绝对路径>`

## 测试根

| 用途 | 路径 |
| --- | --- |
| SSD 测试根 | `<例如 F:\Gallery-Test-Lab-SSD>` |
| HDD 测试根 | `<例如 G:\Gallery-Test-Lab-HDD>` |
| SSD 测试根曾用名（重构前） | `<例如 F:\Gallery-Stage3-SSD-Performance-Test>` |
| HDD 测试根曾用名（重构前） | `<例如 G:\Gallery-Stage3-HDD-Performance-Test>` |

## 10 个目标来源的物理路径

| 逻辑 ID | 物理路径 |
| --- | --- |
| pixiv | `<路径>` |
| pixivFANBOX | `<路径>` |
| Gank | `<路径>` |
| Fantia | `<路径>` |
| Patreon | `<路径>` |
| Pawchive | `<路径>` |
| X | `<路径>` |
| 微博 | `<路径，必须是 G 盘物理路径，不得经由链接>` |
| 微博_Legacy | `<路径，必须是 G 盘物理路径，不得经由链接，且与「微博」相互独立>` |
| Venera | `<路径>` |

## 各阶段目录（在每个测试根下）

```text
<测试根>\shared\{bin,rules,fixtures,schemas,scripts}
<测试根>\stages\stage3\{appdirs,logs,manifests,metrics,reports,runs,temp,fixtures}
<测试根>\stages\stage4\{appdirs,logs,manifests,metrics,reports,runs,temp,fixtures}
<测试根>\archive\pre-testlab-restructure\
```

## 固定 Go 工具链

- Windows：`<例如 C:\Users\<用户名>\AppData\Local\CodexToolchains\go1.26.5\go\bin\go.exe>`
- WSL：`<例如 $HOME/go-sdk/bin/go>`

## 仓库外冻结备份

- `tools/stage4` 源码快照路径与 SHA-256：`<路径 + 哈希>`

## 本轮删除的 ≥1M 路径清单

- 见 `Documents/本地/≥1M测试清理清单.local.md`（同样被忽略，不提交）。

## 其它本地输出目录

- `<按需记录>`
