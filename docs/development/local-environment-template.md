# 本机环境模板

复制本文件为 `docs/development/local-environment.local.md` 后填写。本地副本已被 `.gitignore` 排除，不要在模板或提交中写入真实媒体路径、凭据或设备隐私信息。

## 工具链

| 项目 | 本机值 |
| --- | --- |
| Windows 版本 | `<填写>` |
| 架构 | `x64` |
| Go | `1.26.5` |
| PowerShell | `<填写>` |
| Node.js（仅 Web） | `<填写或不适用>` |
| npm（仅 Web） | `<填写或不适用>` |

## 隔离目录

| 用途 | 路径 |
| --- | --- |
| 开发 AppRoot | `<绝对路径，不得位于 Source 内>` |
| 合成测试根 | `<绝对路径>` |
| 便携制品输出 | `<绝对路径>` |
| 临时验证输出 | `<绝对路径>` |

## 真实 Source 授权记录

默认留空。只有任务明确授权真实 Source 验证时，记录脱敏代号、只读方式、guard 方法和允许的扫描范围，不记录公开报告不需要的完整路径。

| 代号 | 存储类型 | 授权范围 | 零写入 guard |
| --- | --- | --- | --- |
| `<source-id>` | `<SSD/HDD/SMB>` | `<有界或全量>` | `<方法>` |

## 备注

- 当前只维护 Windows x64 工具链；不要在此添加 Linux/WSL/macOS 同步测试计划。
- 大型结果和数据库不提交；只保留脱敏、可复核的摘要。
