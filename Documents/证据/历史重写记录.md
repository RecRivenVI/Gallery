# Git 历史重写记录

本文件只记录已发生的 Git 历史重写事实、备份位置和恢复方式，不定义任何产品语义或提交规范。当前提交规范以 [AGENTS.md](../../AGENTS.md) 的「Git Commit Message 规范」一节为唯一权威。

## HR-01（2026-07-23）：提交规范修订与全历史重写

### 范围与目的

本轮按用户明确授权，对 `main` 的全部可达历史执行一次性重写，目的是同时消除两个相反的历史缺陷：

- 阶段 5 与阶段 6 各自被压缩成一个把 migration、领域、安全、契约、前端、测试和文档混在一起的巨大提交，无法逐项审查、无法定位失败范围、也无法独立回退；
- 若干连续提交只承担同一次事实同步或即时补漏，单独保留没有回退价值。

本轮不修改任何产品行为。重写核心完成时的源码树与重写前完全一致。

### 基线与结果

| 项目 | 值 |
| --- | --- |
| 重写前 `origin/main` | `f47eb13e5d2cbcdd3de294970f8e56a199818a8e` |
| 重写前提交数 | 150 |
| 重写核心 HEAD（含提交规范修订） | `3844d07acdc45cb217b9371a10d7dd0eb1c60b10` |
| 重写核心提交数 | 166 |
| 根提交 | `c07cc0be1967e9fdfd4309c4532d284cd946cd54`（内容未变，重建后对象一致） |

### 提交归类统计

| 归类 | 数量 | 覆盖的旧提交 |
| --- | --- | --- |
| `KEEP_AND_REWORD` | 127 | 127 |
| `SPLIT` | 29 | 4（2 个巨大提交与 2 个被并入的状态同步提交） |
| `MERGE` | 5 | 11 |
| `FOLD_FIXUP` | 4 | 8 |
| `DROP_REDUNDANT` | 0 | 0 |
| 新增提交规范提交 | 1 | 不适用 |

150 个旧提交全部被覆盖，没有任何旧提交被丢弃。

### 阶段 5 拆分

旧提交 `253f31bd99cf598aa9027252a643c9ee89b8542c`（50 个文件、11861 行新增）按实际编译依赖拆为 14 个切片：安全 Schema 与主体标识、安全错误码契约、本地账户与 Argon2id 与授权判定、API Token、匿名 Share、安全回归测试、资源级授权前置解析、固定 Blob 分享的 Catalog GC 保护、WebSocket 授权与吊销、备份 manifest v2、安全 OpenAPI 与生成客户端、请求边界守卫与安全管理端点、testlab 安全回归、阶段文档收口（并入原 `10cd675`）。

`internal/auth` 的会话、授权、口令与账户实现互相引用同一批包内符号，无法在文件粒度上继续拆分；会话吊销签名变更同时要求在同一提交内适配既有调用点。该边界由逐提交 `go build ./...` 与 `go vet ./...` 结果确定。

### 阶段 6 拆分

旧提交 `053ab8253f5352ae0b195175d9e9d0e5813ad9ca`（98 个文件、22326 行新增）拆为 15 个切片：前端工程基线、后端集合与脱敏 API、OpenAPI 类型与前端客户端、设计令牌与基础组件、认证与实时通道与应用外壳、浏览与媒体页面、任务与资料库页面、规则表单页面、治理与安全页面、路由装配与静态壳更新提示、浏览器 smoke 门禁、同源静态资产嵌入、真实后端端到端门禁、统一 CI 门禁、ADR-009 与阶段文档收口（并入原 `cbdf9a2`）。

排序由真实依赖决定：应用外壳引用实时通道，因此实时通道与认证同切片；`web/index.html` 与浏览器入口同切片，使前端生产构建从路由装配切片起即可执行；静态资产 `internal/webapp/dist` 是整个前端的构建产物，因此嵌入切片排在全部前端源码切片之后。各前端单元测试与其被测实现同切片。

### 合并与折叠

- 三个描述同一次审计事实的文档提交合并为一个审计事实基线提交；
- 修复后的两个文档同步提交合并为一个缺陷关闭记录提交；
- 阶段 4 压力测试结论的两个文档提交合并为一个；
- 提交规范重写及其相邻修正合并为一个；
- Seed 去重测试的两个相邻收敛提交合并为一个；
- 媒体验证边界的 ADR 调整折叠进其实现提交；
- WSL race 门禁复跑的验证追加折叠进相邻的同文件验证记录提交；
- 中文引号方向修正折叠进被修正的规范提交；
- Issue Form 的 YAML 与链接修正折叠进创建该模板的提交。

`ci(工作流)` 的 Go 包范围修正与 `test(网页)` 的真实后端实时通道门禁经 diff 复核确认属于两个互不依赖的问题，保持独立提交。EV-39/EV-40 一类的后期审计修复全部保持独立提交，发现、修复与关闭的因果顺序完整保留。

两个把 `AGENTS.md` 与代码文件放在一起的旧文档提交未被拆分：其代码部分分别是 2 行 OpenAPI 版本号更正与一处纯 gofmt 对齐，单独成提交会违反本规范对纯格式提交与无独立回退价值微提交的禁止，且两者都已在原提交正文中如实登记。

### 验证结论

- 重写核心中不含提交规范修订的部分，其 tree 与重写前 `origin/main` 的 tree 完全一致；
- 全部提交均包含 `gpgsig` 且 SSH 签名校验为有效；
- 全部提交通过提交信息格式与粒度 lint；
- 阶段 5 的 14 个切片与各阶段边界均通过 `go build ./...` 与 `go vet ./...`；
- 阶段 6 自前端源码出现起的每个切片均通过 `tsc --noEmit`，自路由装配切片起通过 `npm run build`；
- 最终 HEAD 通过仓库完整门禁与 `govulncheck`。

重写核心之后只追加了本记录与失效提交引用的更新。除此之外没有任何产品代码净变化。

### 备份与恢复

重写前已建立三重备份，均在重写前完成校验：

| 备份 | 位置 |
| --- | --- |
| 远端备份分支 | `origin/backup/pre-history-rewrite-20260723-221116` |
| 远端备份 Tag | `history-backup-2026-07-23-f47eb13e5d2c` |
| 本地备份引用 | `refs/backup/pre-history-rewrite/20260723-221116/main` |
| 仓库外 bundle | `Gallery-history-backup-20260723-221116-f47eb13e5d2c/Gallery-before-rewrite.bundle` |
| 仓库外镜像克隆 | `Gallery-history-backup-20260723-221116-f47eb13e5d2c/Gallery-before-rewrite.git` |

bundle 的 SHA-256 为 `BF6E985A631ABDA46B413DBBD5C492253A83A063CD0B3384DBFEAB41BE9E91A9`。完整的 old→new 提交映射保存在同一备份目录的 `mapping.csv`，不写入仓库。

恢复旧历史的任一方式：

```bash
# 从远端备份分支恢复
git fetch origin backup/pre-history-rewrite-20260723-221116
git branch restore-old-main FETCH_HEAD

# 从仓库外 bundle 恢复
git bundle verify <备份目录>/Gallery-before-rewrite.bundle
git fetch <备份目录>/Gallery-before-rewrite.bundle main:restore-old-main

# 从镜像克隆恢复
git fetch <备份目录>/Gallery-before-rewrite.git main:restore-old-main
```

上述备份在用户另行决定前不得删除。

### 已知遗留

- `Documents/证据/验证记录.md` 中的 `d3670c9` 与 `PROJECT_STATUS.md` 中的 `083877a9e022d5f0f58dc59a5e91fda9254ea7c3` 在本轮重写之前就已不在 `origin/main` 的可达历史中，属于更早一次历史整理遗留的失效引用。本轮不改写这两处历史证据文字，只在此登记该事实。
- `Documents/证据/验证记录.md` 第 686 行存在一处随原提交进入历史的行尾空白。修正它会破坏与重写前源码树的等价性，因此本轮保留原样并在此登记。
- 阶段 3 的 `fix(任务): 完成阶段 3 任务与维护 Correctness`、阶段 4 的 `fix(查询): 收敛媒体快照绑定与字段级排序等阶段 4 Correctness 缺口` 与 `fix(扫描): 修正扫描默认档案策略、严格校验、TOCTOU 复用窗口与确认时间语义` 仍是跨多个子系统的大提交，已按拆分标准登记为候选但本轮未拆分；阶段 5 的 `feat(接口): 建立请求边界守卫与安全管理端点` 同时包含请求边界守卫、既有端点授权改造与新增安全端点，也仍是候选。它们的拆分需要单独一轮历史重写任务。

## HR-02（2026-07-24）：提交规范重写、全历史文案规范化与四个大提交拆分

### 范围与目的

本轮按用户明确授权，对 `main` 的全部可达历史执行第二次一次性重写，目的是：

- 整体重写 `AGENTS.md` 的「Git Commit Message 规范」区域，建立单一、自然、无重复且可机器检查的提交文案与排版规范；
- 依据新规范逐条复核并规范化全部历史提交信息，统一阶段编号、中英文与数字空格、全角半角标点、连续空格与不可见字符；
- 拆分 HR-01 已登记为粒度过大的四个历史提交。

本轮不修改任何产品行为。重写核心（不含 `AGENTS.md` 规范修订与本记录）完成时的源码树与重写前完全一致。

### 基线与结果

| 项目 | 值 |
| --- | --- |
| 重写前 `origin/main`（OLD_MAIN） | `2edee271a00c30eef516a0dd4289a432a6000eb2` |
| 重写前提交数 | 167 |
| 重写核心 HEAD（含 `AGENTS.md` v2 规范修订，不含本记录） | `18f3d411db92ed01743bdcc3008ef90282e1e45e` |
| 新根提交 | `c99bcbd2a70ebd56adc0eaf5d6fc4b378153afde`（内容未变，重新签名重建） |
| OLD_MAIN 源码树 | `ceb92897f6688f44525eec2f17747e77044a93e1` |

### 提交归类统计

| 归类 | 数量 |
| --- | --- |
| `KEEP_AND_REWORD`（信息保持等价或仅修排版） | 163 |
| 其中因排版规范修正而改写文案 | 9 |
| `SPLIT`（四个大提交拆分） | 4 → 14 个切片 |
| 新增 `AGENTS.md` 规范修订提交 | 1 |
| 新增本历史重写记录提交 | 1 |

167 个旧提交全部一对一映射到重写后提交，除四个拆分目标外没有再执行任何合并、删除或粒度重构；没有任何旧提交被丢弃。

### `AGENTS.md` 规范修订摘要

将「Git Commit Message 规范」整区替换为从上到下自然阅读的单一规范，新增：字符编码与不可见字符约束、阶段编号唯一写法（`阶段 N` 与 `阶段 M～阶段 N`）、汉字与拉丁字母与数字之间的空格规则、行内代码与技术标识的排版、中文标点与括号引号与范围号规则、语义真实性、人工审查与机器检查并列的强制要求，并用中性抽象案例替换与具体实现绑定的正反例。

### 阶段编号与排版规范化结果

对全部 167 条历史信息建立自动化 lint 并逐条复核，共修正 9 条提交的排版问题：阶段中文数字（`阶段五`、`阶段六`）、阶段连续范围的半角连字符与短破折号（`阶段 0-3`、`阶段 0–3`）、斜杠并列阶段（`阶段 3/4`）、`阶段` 与中文数字相邻（`执行阶段一致性`）、中文语句中的半角引号。重写后全历史 lint 结果为 `0 violations`。

### 四个大提交拆分

| 原提交（HR-01 SHA） | 主题 | 切片 |
| --- | --- | --- |
| `c43896e` | 阶段 3 任务与维护 Correctness | 4：任务 Attempt/Temp/退避持久状态、维护互斥与空间估算与所有权 GC、派生与工具任务资源预检、非阻塞调度与强杀恢复 |
| `2d9946f` | 阶段 3 扫描默认档案与 TOCTOU 与确认时间 | 5：投影内容确认状态列解耦、扫描默认档案读取入口、档案严格校验与自动选择、复用前重新校验阻断 TOCTOU、确认时间只在完成哈希时推进 |
| `22ff35b` | 阶段 4 查询 Correctness | 3：字段级排序与动态查询依赖与投影字段、媒体与派生资源快照绑定、单媒体确认目标化 |
| `221aca7` | 阶段 5 接口 | 2：请求边界守卫与安全管理端点、既有端点按 capability 与资源范围授权改造 |

每个切片均按真实编译依赖排序，逐切片通过 `go build ./...` 与 `go vet ./...`，各拆分目标切片净 diff 之和等于原提交净 diff（最终树逐个校验一致）。四个原提交的生成客户端（`pkg/galleryapi/openapi.gen.go`）为单次重新生成产物，其内容跨多个关注点且各请求构建函数的签名与函数体互相引用，无法在不破坏生成一致性的前提下按关注点拆分；对 `22ff35b` 保持生成客户端随其首个引入契约变更的切片整体提交，对 `221aca7` 因 `New()` 单点集中注册全部新路由且新端点实现为单个连续 diff 块而将请求边界守卫与全部安全管理端点合并为一个切片。这些无法进一步拆分的边界均以逐切片 `go build` 结果证明。

### 签名与验证

- 全部重写提交均通过 `git commit-tree -S` 使用 SSH 密钥重新签名，commit object 均含 `gpgsig`；
- 重写核心（`18f3d411`）之前的第 167 个提交源码树与 OLD_MAIN 源码树完全一致（`ceb92897`）；
- `AGENTS.md` v2 提交的源码树等于「OLD_MAIN 加完整规范重写」的目标树（`5883eb95`）；
- 除 `AGENTS.md` 规范区域与本历史重写记录外，产品目录净变化为空。

### 备份与恢复

上一轮 HR-01 的全部备份保留不变、未修改、未覆盖。本轮另建第二代独立备份：

| 备份 | 位置 |
| --- | --- |
| 远端备份分支 | `origin/backup/pre-history-rewrite-v2-20260724-010518` |
| 远端备份 Tag | `history-backup-v2-2026-07-24-2edee271` |
| 本地备份引用 | `refs/backup/pre-history-rewrite-v2/20260724-010518/main` |
| 仓库外 bundle | `Gallery-history-backup-20260724-010518-2edee271/Gallery-before-rewrite-v2.bundle` |
| 仓库外镜像克隆 | `Gallery-history-backup-20260724-010518-2edee271/Gallery-before-rewrite-v2.git` |

bundle 的 SHA-256 为 `A1628B65A32BC1716A5B647865C0F49C900423528F7DAA1367988D78EB36F781`。bundle 与镜像均已在独立空仓库中解析出 `2edee271` 并校验提交数 167。完整的 HR-02 old→new 提交映射保存在同一备份目录的 `mapping.csv`，不写入仓库。

恢复 OLD_MAIN 的任一方式：

```bash
# 远端备份分支
git fetch origin backup/pre-history-rewrite-v2-20260724-010518
git branch restore-old-main FETCH_HEAD

# 仓库外 bundle
git bundle verify <备份目录>/Gallery-before-rewrite-v2.bundle
git fetch <备份目录>/Gallery-before-rewrite-v2.bundle main:restore-old-main

# 镜像克隆
git fetch <备份目录>/Gallery-before-rewrite-v2.git main:restore-old-main
```

上述备份在用户另行决定前不得删除。

### 与 HR-01 的世代关系

HR-01 记录中的全部 SHA（根提交 `c07cc0be`、重写核心 `3844d07`、被拆分的 `253f31bd`/`053ab82` 等）均属于 HR-02 之前的上一代历史，本轮不改写 HR-01 记录当时的事实陈述。HR-02 之后当前历史的根提交为 `c99bcbd2`，重写核心 HEAD 为 `18f3d411`。`验证记录.md`、`PROJECT_STATUS.md` 与 `阶段3-4大规模测试归档.md` 中以 `c07cc0be`、`ae46e0e`、`f80c810` 等标注的条目是记录当时审计基线的历史证据，属于上一代历史 SHA，其对应的 HR-02 新 SHA 见备份目录的 `mapping.csv`；为不篡改历史证据的当时事实，本轮不就地改写这些证据文字。

### 未执行的产品门禁

本轮不改变产品源码树，因此未重新执行 500,000 规模性能门禁、真实 HDD/SMB/NAS、物理 LAN 多设备、目标低端设备 Argon2id、真实恶意资源、Firefox 与真实移动设备/屏幕阅读器等门禁；这些门禁的状态以 HR-02 之前的验证记录为准，未因本轮历史重写而变化。
