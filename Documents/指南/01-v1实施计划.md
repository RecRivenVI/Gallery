# v1 实施计划

> 类型：实施指南。开发顺序、首条垂直切片和 v1 交付边界以本文为唯一权威来源。

## 实施策略

先建立一条能证明核心边界的纵向链路，再扩展平台和 UI。不得先写完整前端、完整表结构或全部平台规则，最后才验证数据所有权和发布一致性。

建议源码职责布局如下，具体包名可调整：

```text
gallery/
  cmd/                 # galleryd, galleryctl
  domain/              # 实体、值对象、端口
  application/         # 用例与事务边界
  rules/               # Schema、编译器、Rule IR、Trace/Impact
  control/             # 不可重建事实仓储
  catalog/             # revision、查询投影、搜索
  scanner/             # 发现、候选关联、staging 构建
  jobs/                # 持久任务与有界调度
  media/               # Range、ETag、DerivedAsset
  transport/           # HTTP、OpenAPI、WebSocket
  platform/            # OS 适配器
  web/                 # Web/PWA
  shell/               # 可选薄壳
```

这只是模块边界，不冻结类名、函数名或表名。

## 阶段 0：契约骨架

> 实施状态（2026-07-16）：已完成并通过正式仓库自动化门禁；该状态不冻结最终物理 Schema。

先交付：

- 领域 ID 和 Source/CanonicalWork/CanonicalCreator/CanonicalMedia/ContentBlob/FileLocation 的最小值对象；
- `control.db` / `catalog.db` 的 Schema 升级框架、备份接口和独立生命周期；
- OpenAPI、错误 code、WebSocket 信封、排序协议和游标的可执行 Schema；
- 规则包 JSON Schema、编译器版本和 Gallery CEL Profile；
- AppDirs 与媒体根写入守卫；
- 可注入的时钟、ID、文件系统和进程端口，便于确定性测试。

完成条件：规范中的实体和错误可由契约测试引用，但尚不冻结最终物理表结构。

## Walking Skeleton

> 实施状态（2026-07-16）：已完成。一个作品/一个媒体的仓库内合成只读 Source 已贯通 Personal 配对、Library/Source、RuleVersion/SourceRuleBinding、持久 Job、完整 SHA-256、Canonical Binding、staging publication、REST、媒体 HEAD/GET/Range、WebSocket 和服务重启恢复。证据与局限见 [验证记录 EV-13](../证据/验证记录.md#ev-13-walking-skeleton-e1--e4e5)。

使用 Test-Bench 内仅含一个作品和一个媒体的合成只读 Source，尽早打通最小纵向链路：

1. 启动 `galleryd`；
2. Personal 模式建立最小管理 Session；
3. 创建一个 Library 和一个 Source；
4. 创建 SourceRuleBinding，绑定并编译最小 RuleVersion/RuleParameters；
5. 扫描一个 SourceWork 和一个 SourceMedia，对媒体完成首次完整 SHA-256；
6. 构建最小 Catalog revision 和匹配的 Overlay projection revision，在短事务内创建 `query_publication_id` 并切换活动 publication；
7. 通过生成的 OpenAPI 客户端和 REST 获取一个 CanonicalWork，响应携带 `query_publication_id`；
8. 通过稳定 CanonicalMedia ID 执行一次 HEAD 和单区间 Range；
9. 通过 WebSocket 收到 Job completed，并能用 HTTP Job snapshot 复核；
10. Web、CLI 或最小测试客户端全程不访问数据库或后端内部包。

Walking Skeleton 只证明模块依赖方向、两库基础生命周期、API-first、OpenAPI 生成、AppDirs、最小扫描 publication 和客户端边界。它暂不要求完整 FTS、快照分页、Overlay 写入/Catalog 重建、多个强杀点、完整 RuleImpact、多平台或真实大库性能，不能据此冻结物理 Schema 或完整 API。

功能可以精简，基础模型不能临时替代：必须使用正式 `control.db`/`catalog.db` 迁移框架、Canonical/Source 稳定 ID、SourceRuleBinding、ContentBlob 完整哈希身份、`query_publication_id`、正式错误信封/OpenAPI DTO 和 capability 检查。禁止用以后需要替换的内存数据库、路径主键、临时 DTO、匿名管理员或客户端直连数据层“先跑通”。Walking Skeleton 不冻结全部表和字段，但已选中的身份、所有权、授权与 publication 契约只能通过兼容迁移扩展，不能在 Architecture Proof 时推翻。

## Architecture Proof Slice

> 2026-07-16 状态：本切片的合成正确性门禁已经通过，证据见 [验证记录 EV-14](../证据/验证记录.md)。第一版物理 Schema 与完整 API 暂不冻结；下一步进入阶段 1 的 Canonical/Binding 人工修复和多 Source 领域闭环，同时保留百万级查询、Reference Performance 和平台退化门禁。

Walking Skeleton 通过后，以下完整切片随阶段 1–4 逐步补齐，并作为数据库 Schema 与查询/API 契约冻结前的架构验收：

使用 Test-Bench 内的合成只读 Source，完成以下端到端路径：

1. Personal 模式启动 `galleryd`，一次性配对取得管理 Session；
2. 创建一个 Library 和一个 Source，以 SourceRuleBinding 绑定一个最小 RuleVersion 与参数；
3. validate/compile/Dry Run，得到 Rule IR、Trace 和 Impact；
4. 创建 Scan Job，分批构建 staging revision；
5. 在短事务内发布 SourceWork/SourceCreator/SourceMedia、ContentBlob/FileLocation，以及匹配的 WorkProjection/CreatorProjection/MediaProjection、SortKey 和 FTS；Canonical 实体仍以 `control.db` 为权威；
6. 通过 OpenAPI 浏览、过滤、搜索、排序，并以绑定合法 revision 组合的 `query_publication_id` 快照分页；
7. 通过稳定 CanonicalMedia ID 执行 HEAD、Range、ETag 和条件请求；
8. WebSocket 观察任务并在断线后从 HTTP snapshot 恢复；
9. 设置标题 Override 和 Favorite，等待 Overlay projection 发布；删除并重建 `catalog.db`；证明稳定 Binding、Override、Favorite 和固定 Blob 引用恢复；
10. 在构建中、发布前和发布后强杀进程，验证旧/新 revision 与 Job reconciliation。

Architecture Proof 通过条件：媒体 Source 前后清单一致；没有绝对路径或 Catalog 内部 row ID 出现在公共 API/永久 control 事实；所有失败均有稳定 code；客户端没有直接数据库或内部包依赖；查询全程不混合 Catalog/Overlay/FTS 代次。

本轮实现已经证明：正式规则包可经 API 完成 validate/compile/Dry Run/Trace/Impact；查询通过服务端签发的 `query_publication_id` 固定 Catalog/Overlay/FTS 代次并以签名 cursor 续页；query-affecting Overlay 使用持久 Job 重投影；删除 `catalog.db` 后 Canonical Work/Creator/Media、稳定 Binding 和用户事实可恢复；Blob/Location/DerivedAsset 与旧 revision 受读取租约保护；八个独立子进程强杀点均能恢复或以稳定错误收敛。该结论只提升 Architecture Proof 的 Correctness 状态，不替代下面各阶段的完整领域、性能、安全和平台门禁。

冻结评估采用分层结论：forward-only migration 的事务/checksum 规则、`query_publication_id` 合法 revision 组合语义、WebSocket v1 信封、RuleVersion/Rule IR 三类身份以及 Media/Blob/Location 稳定引用可以作为兼容演进基线；`control.db`/`catalog.db` 最终物理约束、完整 REST 资源、排序/过滤全集和 cursor 内部格式仍不得冻结。阻塞项包括 Work/Media 误绑定修复、超大文件持久哈希 Job、100 万/1000 万正式查询语料、ranking/highlight/total 的 API 表达、1%/10%/50% publication 参考性能、HDD/NAS/磁盘满退化以及目标平台安全门禁。CanonicalCreator 合并/撤销已在阶段 1 落地，见下。

## 阶段 1：领域和数据所有权

> 实施状态（2026-07-18）：**阶段 1 已完成**。CanonicalCreator 合并/撤销（[EV-15](../证据/验证记录.md)）、Work/Media/Creator Binding issue 的发现/查询/人工修复与 Source-derived inactive 生命周期（[EV-16](../证据/验证记录.md)）、基于成功扫描计数的 orphaned 保留窗口与 orphan candidate 人工审查（[EV-17](../证据/验证记录.md)）、control.db 产品级备份/恢复与升级失败回滚及 Catalog 全量重建后的人工决策恢复端到端门禁（[EV-19](../证据/验证记录.md)）、SourceWork 拆分/合并检测/决策与仅限未消费决策的撤回及其 Catalog 全量重建恢复门禁（[EV-20](../证据/验证记录.md)）均已落地并配套 API。阶段 1 Schema Freeze Gate 已执行：身份与唯一约束的逐项分类登记在 control 迁移 `00016_schema_freeze_phase1` 的 `schema_freeze` 表，`(source_id, source_key) WHERE status='active'` 等核心身份约束已冻结，其余以 COMPATIBILITY_BASELINE/PRE_FREEZE/DEFERRED 记录并仍可经 forward-only migration 演进。下一阶段为阶段 2 规则闭环。

- 完成 Library/Source/Provider、SourceWork/SourceCreator/SourceMedia、CanonicalWork/CanonicalCreator/CanonicalMedia、Binding 和 Overlay 的仓储；
- 实现 CanonicalCreator 合并/撤销、SourceWork inactive/orphaned 和人工 Binding review；
- 实现 CanonicalMedia/ContentBlob/FileLocation/DerivedAsset 身份与完整 SHA-256 确认；
- 实现 SourceWork 拆分/合并检测、人工决策与仅限未消费决策的撤回，复用 Binding issue 与 pre-seed Binding；
- 建立 control 备份/恢复、Catalog 全量重建与 split/merge 决策恢复测试；
- 通过 Schema 冻结门禁，逐项分类唯一约束（见 `schema_freeze` 表）。

## 暂定实装决策

下表登记「已实装但未冻结」的常量与选择，避免把临时实现误当已冻结决策。状态含义见 [文档导航状态词](../README.md#状态词)：临时=可能整体替换；兼容演进基线=以兼容迁移演进；有条件接受=达门禁前不冻结。修改表内项前先确认其重新审议门禁。

| 项目 | 当前实现 | 状态 | 最迟冻结阶段 | 重新审议门禁 | 升级/迁移路径 |
| --- | --- | --- | --- | --- | --- |
| Binding active 唯一约束 | `(source_id, source_key) WHERE status='active'` | 已冻结（FROZEN） | 阶段 1 Schema Freeze（已完成） | 拆分/合并、多 Source 证据齐备，已冻结为身份不变量 | 见 `schema_freeze`；破坏前须新 ADR |
| orphan 默认阈值 | 3 次连续缺失的成功扫描 | 临时（阶段 1 门禁评估为可调常量，未冻结） | 阶段 2 运行调优 | 真实误报/漏报比例 | 常量或 Source 配置调整，不改身份 |
| orphan 保留延长 | 每 Binding `retention_scans_override` | 临时（阶段 1 门禁评估为可调语义，未冻结） | 阶段 2 运行调优 | 审查工作量证据 | 列已存在，语义可调 |
| cursor lease | 5 分钟 | 有条件接受 | 阶段 4 | 并发游标 GC 与长查询证据 | 运行配置 |
| DerivedAsset read lease | 5 分钟 | 有条件接受 | 阶段 4 | 大文件/慢盘读取证据 | 运行配置 |
| Publication 读取 lease（`PublicationReadLeaseDuration`） | 2 分钟，仅显式快照模式媒体读取需要 | 有条件接受 | 阶段 4 | 大文件/慢盘快照读取证据 | 运行配置 |
| Ranking 字段权重 | 标题优先级 3、Creator 2、Tag 1、文件名 0；`match_class`（0-3）× 10 + 优先级 | 有条件接受（v2 结构已冻结，数值 PRE_FREEZE） | 阶段 4 正式压力测试 | 真实语料下的字段相关性证据 | 调整优先级常量需同步递增 `rankProtocolVersion` |
| Personal pairing lifetime | 5 分钟 | 有条件接受 | 阶段 5 | 浏览器配对可用性 | 运行配置 |
| Session lifetime | 30 天 | 有条件接受 | 阶段 5 | LAN 账户与吊销策略 | 运行配置 |
| Blob 算法 | `sha256-v1` | 兼容演进基线（阶段 1 未冻结，待哈希升级门禁） | 领域 Schema 最终冻结门禁 | 哈希升级需求 | 算法版本并存 + 重哈希 Job |
| Location key | 相对路径版本化摘要 | 兼容演进基线（阶段 1 未冻结，待文件身份门禁） | 领域 Schema 最终冻结门禁 | SMB/inode 复用/无 FileID 证据 | identity_version 演进 |
| Overlay 字段能力与 dependency set | `overlay.OverlayFieldCapabilities` 静态能力注册表（Display/Filter/Sort/Search/Visibility/ResourceReference/LiveUserState）+ `query.Service` 按实际请求生成的动态 `dependencySet`（不再是全局静态 snapshot/live 二元分类）；Favorite/Progress 因新增 `overlay.favorite`/`overlay.progress` 过滤字段已进入写后屏障，同时保留 `LiveUserState` 额外展示 | 兼容演进基线（阶段 4 已落地代码主线，字段覆盖范围仍可扩展） | 阶段 4 正式压力测试 | 更多字段进入过滤/排序/搜索/集合判断 | 扩展能力注册表与 planner 分支，不改变既有字段语义 |
| media.derive capability | 独立于 `media.read`，Personal 单一 owner 角色默认同时拥有 | 兼容演进基线 | 阶段 5 多账户 | 只读媒体账户与生成配额需求 | 阶段 5 引入账户后按角色裁剪 |
| SourceRuleBinding 执行 | 只选择 active、条件匹配后 priority 最高且 `binding_id` 稳定排序的一条 Binding；同一 Source 同一 priority 冲突；参数集 revision/hash 在 Binding 与 Job 入队时冻结 | 兼容演进基线（阶段 2 已落地，单 Binding 语义仍未最终冻结） | 阶段 3 扫描规则调度门禁 | 多规则链/Provider 路由需求 | 扩展执行模型，不改身份 |
| Job 执行 | 中央有界 scheduler（scan/hash/overlay/derived/external-tool/maintenance 六类独立并发池；非阻塞提交；同一逻辑 Job 多 Attempt；周期租约回收、退避、进度和取消） | 兼容演进基线（阶段 3 合成 Correctness 已修正） | 阶段 3 | 真实 HDD/SMB/NAS、网络挂载、池公平性与正式性能证据 | 注册新类别与 Runner |
| Rule extension 分类 | required/optional × semantic/nonsemantic；注册表 `{gallery.identity: v1}`，semantic `gallery.identity` 支持受限 stable key 前缀 | 兼容演进基线（阶段 2 已落地，注册表仍未最终冻结） | 阶段 3 真实 Source/调度门禁 | 新 semantic extension、注册表版本升级 | 扩展注册表；遗留 extension 按 optional+nonsemantic 兼容 |
| RulePackage 生命周期 | `control.db` 保存 Package/Draft/Version/Parameter/Test/Audit；草稿 revision CAS，发布版本不可变，回滚只切换当前引用并审计 | 兼容演进基线（阶段 2 已落地） | 阶段 3 规则重扫与备份恢复门禁 | 多包共享版本、删除和升级恢复证据 | forward-only migration，旧版本保持可读 |
| RuleImpact 分类 | `NO_ACTION`、`REPROJECT`、`RESCAN_PARTIAL`、`RESCAN_FULL`、`BINDING_REVIEW`；输出字段、原因、hash 和任务估计 | 兼容演进基线（阶段 2 已落地） | 阶段 3 调度联动门禁 | 真实 Source 变化比例和任务幂等证据 | 扩展 action/category，不重写历史 RuleVersion |
| Rule 参数身份 | 参数集以 revision/hash 变化，Binding 允许一层 canonical override，Job 冻结有效参数和 compiler/Profile/registry | 兼容演进基线（阶段 2 已落地） | 阶段 3 扫描重试和回滚门禁 | 参数命名空间、跨版本兼容和共享参数规模 | 新 revision 保留旧 revision，Binding/Job 按快照读取 |
| AppDirs 单写者锁 | Unix flock / Windows LockFileEx，锁文件位于 Runtime | 兼容演进基线 | 阶段 7 | SMB/NAS 与容器挂载的锁行为 | 平台 adapter 替换 |
| OpenAPI 路由与 DTO | pre-alpha 兼容基线（`0.5.0-pre-alpha`） | 兼容演进基线 | 阶段 4 | 完整排序/过滤/total/highlight 语义 | 兼容扩展，破坏性变更前先升版本策略 |
| control 备份 manifest | `manifestVersion=1`，含 role/schemaVersion/migrationChecksum/checksum/安全范围 | 兼容演进基线（阶段 1 未冻结） | 阶段 5 安全 | 需新增备份内容或跨版本恢复格式演进 | 提升 manifestVersion 并保持旧版可读 |
| 备份安全范围 | Session/pairing 以 SHA-256 摘要随备份并在恢复时作废；无 API Token/CredentialStore | 临时 | 阶段 5 | LAN 账户、API Token 与 CredentialStore 落地 | 扩展安全范围声明并定义各自恢复策略 |
| 恢复应用模型 | 请求登记 + 下次启动在单写者锁下打开数据库前原子替换 | 兼容演进基线 | 阶段 7 | 在线热替换或多进程恢复需求 | 平台 adapter 与恢复编排演进 |
| SourceWork 结构检测证据 | 媒体 ContentBlob digest 集合交集，拆分=1→多、合并=多→1 | 兼容演进基线 | 阶段 2 | 缺 Blob 证据来源、非内容型拆分识别需求 | 扩展证据集合，不改身份 |
| 结构决策动作集合 | split inherit/keep_same/create_new、merge bind_existing/create_new、defer、仅限未消费决策的 undo | 兼容演进基线 | 阶段 2 | split.bind_existing 分支、显式 Canonical merge 需求 | 扩展动作，pre-seed Binding 模型不变 |
| scanProfile 默认值 | 未显式指定时按 Source 是否已有 publication、以及 control.db 是否保留该 Source 的持久领域历史（`work_bindings`/`media_bindings`/`creator_bindings`/Binding issue/SourceWork 结构决策，见 `Resources.SourceHasDurableHistory`）自动选择：均无则选 `index`，任一存在则选 `incremental`；持久化的是最终决定的具体档案，不保存 `auto`。已有 publication，或 Catalog 已丢失但仍有持久领域历史的 Source 显式请求 `index` 返回结构化 `CONFLICT`，不创建 Job | 有条件接受 | 阶段 3 真实规模复测 | 真实规模下 incremental 复用命中率与误判率证据、index 与自动扫描竞争的真实并发场景 | 运行配置/请求参数调整，不改身份 |
| incremental 复用判据 | Source + 规范化相对路径 + size + mtime_ns 组合，既往状态须为 content_verified | 临时（未接入真实平台文件身份） | 领域 Schema 最终冻结门禁（文件身份） | `FileIdentityProvider` 端口与 Windows FileID/dev+inode 适配落地 | 判据集合扩展，不改变 content_verified 只由完整 SHA-256 确认的不变量 |
| container_signature 字段 | 已在 source_media 记录，本轮未用于任何跳过判断 | 临时（占位） | 阶段 3 后续性能优化 | 目录级跳过优化的真实收益证据 | 定义具体签名算法与失效条件后启用 |
| located_unverified 媒体的内容访问 | 允许出现在 Work/Media 列表与基础浏览（`available=true`、`blob=null`、`contentVerificationState=located_unverified`、`verifiedAt=null`；`available` 只表达位置可用性，不因内容未确认而报告为不可用）；GET/HEAD 媒体正文返回专用 `CONTENT_NOT_VERIFIED`，不进入依赖 ContentBlob/ETag 的已验证读取路径，也不与 Source 离线的 `MEDIA_OFFLINE` 混淆 | 兼容演进基线（阶段 3 已落地专用错误码） | 阶段 4 查询和媒体 | 是否允许对 located_unverified 媒体直接流式读取原文件的安全模型裁决 | 新增媒体服务分支，不复用 content_verified 的 ETag/校验路径 |

> 阶段 1 Schema Freeze Gate 已把身份与唯一约束逐项分类登记在 control 迁移 `00016_schema_freeze_phase1` 的 `schema_freeze` 表（FROZEN / COMPATIBILITY_BASELINE / PRE_FREEZE / DEFERRED）。表中标记为 FROZEN 的项视为兼容承诺，破坏前须新增或修订 ADR；其余仍可经 forward-only migration 演进。本节常量若与 `schema_freeze` 冲突，以 `schema_freeze` 为准。

## 阶段 2：规则闭环

> 实施状态（2026-07-18）：**阶段 2 规则闭环已完成 Correctness 实现**。`00017_rules_lifecycle` 增加 RulePackage/Draft/Parameter/Version/Test/Example/Audit/CompilationCache 生命周期；规则包支持严格 JSON/YAML/TOML 导入、Schema/UI metadata、精确十进制数字、未知 optional nonsemantic extension 无损保留、semantic extension 受注册表约束；草稿使用 revision 乐观并发，发布只写不可变 RuleVersion，回滚先比较 Impact 并要求人工确认；Explain/Trace/Diff/Impact、参数影响分类、Binding 状态和 Job 入队规则快照均有持久化/API/测试。百万级性能、真实媒体样本和跨平台发行门禁仍按后续阶段执行，不能据此描述为产品发行就绪。

- 在已有有限原语、CEL Profile 和 Rule IR 基础上完成编译缓存与规则产品闭环，不重新创建第二套执行引擎；
- 支持规则包版本、参数复用、未知扩展无损往返和原子保存；
- 建立表单、文本编辑、Dry Run、Explain、Impact、版本 diff 和回滚；
- 将三类通用目录/metadata 形态转为仓库内嵌合成示例和黄金测试，并提供受限示例测试 API，不写平台分支；
- 对身份规则变化强制 Binding review，对展示变化只重投影。

阶段 2 的实现边界与继续保留的冻结项：规则持久化事实仅进入 `control.db`；编译缓存、Rule IR 和 Impact 计划可丢失并可重建；输入导入不写 Source。`ui_metadata` 只参与 `package_hash`，不参与 `semantic_hash`；tests-only 修改只产生新的 package artifact；当前 Binding 按 active/条件/priority/ID 只选择一条，数据库拒绝同一 Source 同一 priority，多规则链、Provider 路由、参数身份最终命名空间、Impact 与 Job 的自动调度联动和完整表单 UI 留待后续冻结门禁。阶段 2 不宣称已完成真实规模、跨平台或产品发行门禁。

## 阶段 3：扫描、任务和 Catalog

> 实施状态（2026-07-19）：**阶段 3 生产代码和合成 Correctness 已完成，并完成真实 SSD/HDD 大数据集结构抽样、只读清单和有界样本验收引发的扫描档案重构**（全量扫描未完成，正式全量性能 Gate 仍未通过）。`00018_stage3_jobs`/`00019_stage3_correctness` 建立的持久任务基础不变；六类调度池提交不阻塞，周期恢复器统一处理租约、退避与 queued 重提。真实 SSD（pixiv，约 36.6 万文件）与 HDD（微博，约 63.2 万文件）验收发现默认全量完整哈希扫描在真实规模下不适合作为日常路径（真实 HDD 约 8 items/sec，估算需约 22 小时），用户据此终止全量哈希验收，扫描改为三种正式 `scanProfile`：`index`（发现即发布 `located_unverified`，不建立 Hash Job）、`incremental`（默认，按 Source/相对路径/大小/mtime 组合证据复用既往 `content_verified` 观察，仅对新增或变化媒体建立 Hash Job）、`verify`（忽略既往观察强制重新完整哈希，即显式低频完整性校验）。`00008_content_verification` 在 `source_media` 新增观察证据与 `content_verification_state`；`content_blobs`/`file_locations` 结构不变，仅在媒体确认后才写入。真实验收同时发现并修复两个独立缺陷：`internal/rules` 的 `media_order` 按字节字典序排序导致 10+ 分页作品顺序错误（已改用与查询排序协议一致的自然序）；`internal/jobs` 的 `retrySameJob` 未捕获 `jobs_one_active_scan_per_source` 唯一约束冲突，在同一 Source 出现另一活跃扫描时会把裸 SQLite 约束错误当作 `CodeInternal` 传播，导致启动 reconciliation、进而 galleryd 启动本身整体失败（已改为可重试的 `CodeJobStateConflict` 并由 `RequeueDueFailures` 优雅跳过）。真实 HDD、SMB/NAS、网络挂载、真实平台文件身份（FileID/`dev+inode`）以及正式 Reference/Degradation Performance Gate 仍留待后续实测，不能据此描述为阶段 3 的真实规模或发行验收完成；本轮全量完整哈希的中止耗时与真实吞吐样本作为「保守全量哈希方案在真实规模下成本过高」的证据留存，不代表阶段 3 性能失败。2026-07-19 另完成一轮阶段 3 Correctness 小型收尾（见 [验证记录 EV-26](../证据/验证记录.md)）：未显式指定 `scanProfile` 时按 Source 是否已有 publication 自动选择 `index`/`incremental`（不再固定为 `incremental`），非法档案值返回结构化 `VALIDATION_ERROR`，已发布 Source 显式请求 `index` 返回结构化 `CONFLICT`；修复 `incremental` 复用既往摘要前的 TOCTOU 复核缺口；`last_confirmed_at`/`verifiedAt` 只在真正完成完整哈希时推进，复用不再错误刷新确认时间；`MediaProjection` 拆出独立 `content_verification_state`/`verified_at`，`location_status` 恢复为只表达位置可用性；Job API 补齐 `scanProfile` 字段；未确认媒体内容端点改为专用 `CONTENT_NOT_VERIFIED`，不再复用 `MEDIA_OFFLINE`。

2026-07-20 完成阶段 3 最终 Correctness 与 CI 收口（见 [验证记录 EV-27](../证据/验证记录.md)）：修复 `internal/overlay` 的 `Reconcile` 对无 publication 的 queued Job 自行 `Start`、与中央 `internal/recovery` 的 `ListRunnable`/`Submit` 竞争领取同一 Job 的生产状态机缺陷（与 `internal/scanner.Reconcile` 的既有对称设计不一致，是触发 `TestRealProcessKillpointMatrix/overlay_fact_preprojection` 在 Windows/Ubuntu GitHub Actions 上约 22% 概率失败的根因），改为与 scanner 对称、只由中央 Recovery Service 领取；killpoint 测试改用可推进的 `clock.Manual` 并对 Job 恢复引入显式的执行记录与重复领取检测。修正 07-19 收尾的 `scanProfile` 默认选择逻辑：新增 `Resources.SourceHasDurableHistory` 综合 Binding/Binding issue/结构决策判断"Catalog 被删除重建但 control.db 仍保留该 Source 历史"这一此前遗漏的边界情形，默认选择改为同时参照 publication 与持久领域历史，避免重建后被误判为全新 Source 而绕过阶段 1 结构审查；同时修复 `index` 档案在最终定位观察后遗漏同步 mtime、可能持久化"当前 size + 较早 mtime"混合 observation 的缺陷。均通过 Scanner 端到端新增测试覆盖，Windows/WSL2 Debian race/`govulncheck` 全部通过。

2026-07-20 同日完成 Catalog publication 与 Recovery 模型的实质语义收口（见 [验证记录 EV-28](../证据/验证记录.md)）：修复 EV-27 登记为"独立发现、本轮不修复"的缺口——Scan Job 建立 staging candidate 后被强杀，同一逻辑 Job 的后续 Attempt 因 `catalog_revisions.job_id` 的扁平 `UNIQUE` 约束永久无法重建候选。确认 Candidate 归逻辑 Job 所有（同一 `job_id` 任一时刻至多一个候选，由多个 Attempt 共同推进或从头重建），`internal/catalog.Store.BeginCandidate` 改为幂等恢复入口，未发布候选先重置（级联清理从属事实与显式清理 FTS5）再重建，已发布候选返回新增的 `CATALOG_CANDIDATE_ALREADY_PUBLISHED` 交由 Scanner 对账为 completed；`catalog_revisions.job_id`/`query_publications.job_id` 两处扁平 `UNIQUE` 均保持不变，未新增或修改 migration。`partial_staging`/`candidate_complete` 两个此前只验证"保持 failed"的 killpoint 场景现在验证退避到期后自动完成，Windows `-count=100`/`-count=20`、`Check.ps1`/`govulncheck` 与 WSL2 Debian `-race` 定向及全量均通过。阶段 3 至此在 partial staging 可自动重试完成、candidate complete 可自动重试完成、同一 Job 最多一个 publication、旧 publication 始终可读、失败 staging 不泄漏、全量 race 与 Actions 全绿六项条件全部满足后正式收口；SMB/NAS、真实 FileID、container signature、MFT/USN 和正式全量性能 Gate 仍列为未完成。

- 以现有 staging revision、短 publication 和跨库 reconciliation 为基础，补齐持久 Job/attempt、取消、重试、心跳租约、单调进度和恢复；
- 将 Blob 完整哈希、扫描、Overlay、DerivedAsset、External Tool 与 Maintenance 分入独立有界资源池，并保留 Source 只读边界；
- 以 Watcher 作为 dirty/overflow 提示，以周期全量校验作为 Source 收敛事实源，保留 offline/permission/file identity/content change 的结构化状态；
- 提供 Catalog GC 活跃 candidate 保护、dry-run、服务端空间预检、WAL checkpoint、VACUUM、publication 维护互斥和任务所有权临时目录清理；
- 将“媒体已发现”与“媒体内容已完整确认”解耦为 `located_unverified`/`content_verified` 两态，提供 `index`/`incremental`/`verify` 三种扫描档案，默认档案不再要求全量媒体正文读取；
- 下一轮做真实 HDD 有界场景下的 `incremental`/`verify` 性能特征、SMB/NAS、网络挂载、真实平台文件身份接入与正式 Reference/Degradation Performance Gate 实测，不把合成 Correctness 结果扩大为平台或性能结论。

## 阶段 0–3 遗留与后续归属

> 2026-07-20 阶段 4 开工前审计（见 [验证记录 EV-29](../证据/验证记录.md)）：逐项核对本节候选事项与实际代码、迁移和测试，未发现与本文件、`AGENTS.md`、`schema_freeze` 表冲突的漂移。分类含义：`COMPLETE_FROZEN`（已冻结，破坏前需新 ADR）、`COMPLETE_COMPATIBILITY_BASELINE`（可用，经兼容迁移演进）、`IMPLEMENTED_PRE_FREEZE`（已实装，数值/边界未冻结）、`UNIMPLEMENTED_STAGE4_BLOCKER`（阶段 4 前必须处理）、`DEFERRED_TO_LATER_STAGE`（明确延后）、`OPTIONAL_HARDENING`（可选强化，不阻塞）、`DOCUMENTATION_DRIFT`（发现并已修正的文档漂移）。

| 来源阶段 | 事项 | 代码事实 | 当前状态 | 是否阻塞阶段 4 | 最迟处理阶段 | 再审议证据 | 权威文档 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 0 | OpenAPI/错误信封/WebSocket 信封/cursor Schema | `internal/contract/api/openapi.yaml`、`internal/contract/fault`、`internal/contract/realtime/envelope.go`、`internal/contract/query/cursor.schema.json` 均为可执行契约并有契约测试 | COMPLETE_COMPATIBILITY_BASELINE | 否 | 领域 Schema 最终冻结门禁 | 完整排序/过滤/total/highlight 语义落地后再评估冻结 | `AGENTS.md`、`Documents/规范/07-API-实时协议与安全.md` |
| 0 | 最终物理 Schema、完整 REST 资源、cursor 内部格式 | 未冻结，`AGENTS.md` 已如实记录 | IMPLEMENTED_PRE_FREEZE | 否（阶段 4 扩展契约，不违反 pre-freeze 状态） | 领域 Schema 最终冻结门禁 | 阶段 4/5 完整字段集合稳定后 | `AGENTS.md` |
| 0 | 平台端口（FileID/Watcher/ToolDiscovery/CredentialStore） | `internal/platform/{appdirs,descriptor,lock,clock,identity,filesystem}` 已有正式适配器；FileID/Watcher 原生实现、ToolDiscovery、CredentialStore 仍为接口占位 | COMPLETE_COMPATIBILITY_BASELINE（已用部分）+ DEFERRED_TO_LATER_STAGE（未用部分） | 否 | 阶段 3 后续/阶段 7 | 真实 SMB/NAS、目标平台凭据存储需求 | `AGENTS.md` 平台适配边界 |
| 1 | Binding 唯一约束、CanonicalWork/Media/occurrence 语义 | `00016_schema_freeze_phase1` `schema_freeze` 表 FROZEN | COMPLETE_FROZEN | 否 | 已完成 | 需新 ADR 才能修改 | `AGENTS.md`、ADR-002 |
| 1 | FileLocation 最终唯一约束（FileID/SMB/inode） | `Location key` 仍为相对路径版本化摘要，`identity_version` 预留 | COMPLETE_COMPATIBILITY_BASELINE | 否（阶段 4 只读媒体路径不依赖该约束的最终形态） | 领域 Schema 最终冻结门禁 | Windows FileID/Linux dev+inode 适配落地 | `01-v1实施计划.md` 暂定实装决策表 |
| 1 | Windows FileID、Linux dev+inode | 未实现，`platform_identity_kind/value` 列已预留但未写入真实值 | DEFERRED_TO_LATER_STAGE | 否 | 阶段 7 | `FileIdentityProvider` 端口落地 | `AGENTS.md` |
| 1 | external ID 冲突策略 | `source_works_external_idx`/`source_creators_external_idx` 仅建立非唯一索引，无冲突合并策略 | DEFERRED_TO_LATER_STAGE | 否 | 领域 Schema 最终冻结门禁 | 真实多 Provider 冲突样本 | `AGENTS.md` |
| 1 | WorkOrigin/Provider 最终物理模型 | 仅 `source_works.provider_id`/`external_id` 存在；无独立 WorkOrigin 实体；Provider 未投影到 `work_projections`（阶段 4 本轮通过 `internal/query` 与 `source_works` 关联实现只读过滤，见下） | UNIMPLEMENTED_STAGE4_BLOCKER→本轮已用兼容方式解除阻塞（未新增字段，只读关联） | 是（已处理） | 领域 Schema 最终冻结门禁（WorkOrigin 独立模型仍延后） | 真实多 Provider/Origin 混合 Source 样本 | `Documents/规范/06-查询-搜索与排序.md` |
| 1 | orphan 默认阈值/保留延长 | 常量与 per-Binding 覆盖已实装 | IMPLEMENTED_PRE_FREEZE | 否 | 阶段 2 运行调优 | 真实误报/漏报比例 | 暂定实装决策表 |
| 1 | Canonical 删除语义 | 无直接删除 API，只有 merge/unbind/orphan 生命周期 | COMPLETE_COMPATIBILITY_BASELINE | 否 | 阶段 5 账户与数据治理 | 用户级删除请求场景 | `Documents/规范/03-领域模型与数据所有权.md` |
| 1 | Blob hash algorithm version 演进 | `sha256-v1` 固定，无并存/重哈希 Job | COMPLETE_COMPATIBILITY_BASELINE | 否 | 领域 Schema 最终冻结门禁 | 哈希升级需求 | 暂定实装决策表 |
| 1 | 多值字段解析 | `tags_json`、`filenames_text` 已实装并参与查询/搜索 | COMPLETE_COMPATIBILITY_BASELINE | 否 | 已满足阶段 4 需要 | — | `Documents/规范/06-查询-搜索与排序.md` |
| 1 | SourceWork split/merge 的 `bind_existing` 等分支 | 未实现，`01-v1实施计划.md` 已如实记录 | DEFERRED_TO_LATER_STAGE | 否 | 领域 Schema 最终冻结门禁 | 显式 Canonical merge 需求 | 暂定实装决策表 |
| 1 | 显式可撤销 CanonicalWork merge | 未实现（仅 CanonicalCreator 合并已落地） | DEFERRED_TO_LATER_STAGE | 否 | 领域 Schema 最终冻结门禁 | 多 Work 合并证据 | 暂定实装决策表 |
| 1 | CanonicalCreator merge 后按 Creator 浏览/过滤的有效关系 | `work_creator_relations.creator_id` 仍保存合并前 Creator；展示层通过 `ApplyCreatorMerges` 在每次投影重写 `work_projections.creator` 文本；**查询过滤此前完全不存在 creatorId 参数** | UNIMPLEMENTED_STAGE4_BLOCKER→本轮已实现（见下方阶段 4 章节，查询时解析合并等价组，不改写 `work_creator_relations`，保持可逆） | 是（已处理） | 已处理 | — | `Documents/规范/06-查询-搜索与排序.md` |
| 2 | 单生效 SourceRuleBinding | 仍为 active/条件/priority/ID 单一选择 | COMPLETE_COMPATIBILITY_BASELINE | 否 | 阶段 3 扫描规则调度门禁（已过，继续延后至多规则链需求） | 多规则链/Provider 路由需求 | 暂定实装决策表 |
| 2 | 多规则链、Provider 路由、多 Binding 合并 | 未实现 | DEFERRED_TO_LATER_STAGE | 否（本轮不实现，明确禁止） | 未定 | 真实多规则场景 | `AGENTS.md` |
| 2 | 参数身份最终命名空间 | revision/hash 快照已实装，命名空间未最终冻结 | IMPLEMENTED_PRE_FREEZE | 否 | 阶段 3（已过，继续延后） | 跨版本兼容证据 | 暂定实装决策表 |
| 2 | RuleImpact 与自动重扫/重投影调度 | Impact 分类已实装，未与调度器自动联动 | COMPLETE_COMPATIBILITY_BASELINE | 否 | 阶段 3 调度联动门禁（延后） | 真实变化比例证据 | 暂定实装决策表 |
| 2 | extension registry 覆盖范围、CEL 限额、完整表单 UI、WASM/插件 | 均按文档状态未扩大 | DEFERRED_TO_LATER_STAGE / OPTIONAL_HARDENING | 否 | 未定 | — | `AGENTS.md` |
| 2 | 规则 Schema/Impact/UI metadata 供查询字段注册表复用 | 本轮查询字段注册表未复用规则 Schema（R-18/AI 等分类经由规则产出的 `tag` 承载，未新增专用字段），避免扩大规则系统能力 | DOCUMENTATION_DRIFT（登记设计决策，无代码漂移） | 否 | — | — | `Documents/规范/06-查询-搜索与排序.md` |
| 3 | `index`/`incremental`/`verify` 三档案 | 已完成并经真实 SSD/HDD 结构抽样验收 | COMPLETE_COMPATIBILITY_BASELINE | 否 | 已满足阶段 4 前置 | — | EV-25/EV-27 |
| 3 | observation 复用判据（真实 FileID/container_signature/原生 Watcher） | 均未接入真实平台身份，`container_signature` 只是占位字段 | DEFERRED_TO_LATER_STAGE | 否 | 阶段 7/领域 Schema 最终冻结门禁 | `FileIdentityProvider`、原生 Watcher 适配 | 暂定实装决策表 |
| 3 | polling 周期、Job retry/heartbeat/lease、资源池容量 | 已实装为可调运行配置 | IMPLEMENTED_PRE_FREEZE | 否 | 正式性能 Gate | 真实规模并发证据 | `Documents/规范/04-扫描-Catalog与任务.md` |
| 3 | 空间估算系数、Temp/DerivedAsset GC grace | 已实装为可调运行配置 | IMPLEMENTED_PRE_FREEZE | 否 | 正式性能 Gate | 真实磁盘满/慢盘证据 | 同上 |
| 3 | Hash 持久续算、真实 HDD/SMB/NAS/网络盘、MFT/USN、外部工具/ffmpeg | 未完成或未接入 | DEFERRED_TO_LATER_STAGE | 否（本轮明确不测） | 下一轮性能/平台验收 | 真实硬件与网络盘样本 | EV-25、`AGENTS.md` |
| 3 | 完整 Reference/Degradation Performance Gate | 未通过，`AGENTS.md` 已如实记录 | UNIMPLEMENTED_STAGE4_BLOCKER→本轮明确排除在外，不冒充已完成 | 否（任务书明确排除本轮） | 下一轮独立执行 | 真实规模百万/千万语料 + 目标硬件 | `Documents/指南/02-测试与发布门禁.md` |
| 3 | `located_unverified` 按需内容确认闭环 | 此前只有扫描期确认路径，无媒体读取时的按需确认 API；EV-30 首次实现时误把作用范围放大为整个 Source 的 `verify`，EV-31 改为只强制目标媒体（`scanner.CreateVerificationScan`/`verificationTargets`），EV-32 发现冻结的 MediaID/ObservationFingerprint 只被持久化、执行阶段从未真正比对，已补齐目标身份/observation 一致性校验与目标消失检测；EV-33 独立复核这部分确认准确无新缺口，但同一批复核未覆盖 publication 维度——EV-34 发现 `VerificationTarget` 当时未冻结请求所依据的 `queryPublicationId`，HTTP 层用请求 publication 解析媒体身份、却用固定的 active-bound 查询读取 observation，两者可能来自不同快照，执行阶段的 `verifyObservationUnchanged` 同样读取执行时刻的 active publication 而非请求时冻结的那个；已改为 `VerificationTarget` 显式冻结 `queryPublicationId`（按需确认限定为"当前 publication 操作"，显式提供且非当前 active 的历史快照返回结构化 `CONFLICT`）、新增 `catalog.LookupObservationAt` 按指定 publication 查询 observation、执行阶段改用该冻结 publication、幂等键纳入 `queryPublicationId`、前置身份不匹配统一为不可重试的 `VERIFICATION_TARGET_MISMATCH`（不再误用 retryable 的 `CONTENT_CHANGED_DURING_HASH`） | UNIMPLEMENTED_STAGE4_BLOCKER→本轮已实现并修正范围、执行阶段验证与 publication 绑定 | 是（已处理） | 已处理 | — | `Documents/规范/04-扫描-Catalog与任务.md`、`Documents/规范/08-文件系统与媒体处理.md` |
| 3 | 媒体内容和 DerivedAsset 正式公共 API | DerivedAsset 内部服务/Job/缓存/lease/GC 已完成，但生产环境 Resolver 为 `nil`，无公共 HTTP 契约；EV-32 发现异步阶段的 `LocateBlobFile` 仍局限于当前 active publication、且未持有 `BlobReadLease`，已改为跨 revision 内容寻址解析并补齐租约保护；EV-33 独立复核发现 EV-32 的租约保护仍只依赖固定 TTL（排队/退避/生成超过 TTL 时可能被 GC 误回收），且 `derivedjob.Service.Create` 从未设置 `MaxRetries`、退避重试在生产环境从未真正生效，已改为 GC 额外参照非终态/退避等待中的 Job 状态、不依赖固定 TTL 假设，并补齐默认重试策略 | UNIMPLEMENTED_STAGE4_BLOCKER→本轮已实现最小缩略图端到端路径（见下方） | 是（已处理，范围收敛为受限 JPEG 缩略图） | 已处理（完整转码/波形延后） | 完整 ffmpeg 平台发行证据 | `Documents/规范/08-文件系统与媒体处理.md` |
| 3 | 读取 lease 与 Catalog/Blob GC | `blob_read_leases`/`derived_asset_leases` 已实装并与 GC 协作 | COMPLETE_COMPATIBILITY_BASELINE | 否 | 已满足阶段 4 前置 | — | 同上 |
| 3 | 查询、Overlay 和 publication 一致性 | `query_publication_id`/`overlay_projection_revision`/watermark 管线已实装并通过强杀恢复测试 | COMPLETE_COMPATIBILITY_BASELINE | 否 | 已满足阶段 4 前置 | — | `Documents/规范/04-扫描-Catalog与任务.md` |

## 阶段 4：查询和媒体

> 实施状态（2026-07-20，EV-30 主线 + EV-31/EV-32/EV-33/EV-34 Correctness 收尾）：**阶段 4 查询和媒体代码主线及压力测试前 Correctness 收口已完成**（见 [验证记录 EV-30](../证据/验证记录.md)、[EV-31](../证据/验证记录.md)、[EV-32](../证据/验证记录.md)、[EV-33](../证据/验证记录.md)、[EV-34](../证据/验证记录.md)）。已落地：服务端权威结构化查询字段注册表（library/source/provider/creator/creator.role/tag/media.kind/media.locationAvailable/media.contentVerificationState/overlay.favorite/overlay.hidden/overlay.progress）与 AND/OR/NOT 组合、Creator 合并查询时等价组解析（不改写 `work_creator_relations`，撤销后关系照常恢复）、字段级 Ranking v2（标题/Creator/Tag/文件名 `match_class × field_priority` 组合元组，取代 v1 单一标题 tier）、通用版本化命中表达 `matches`（取代仅覆盖标题的 `titleHighlights`，EV-32 修正了截断与 span 越界）、total 三态语义（exact/lower_bound/omitted）、签名 keyset 游标扩展 `rankProtocolVersion`（v2，`lastRankTier` 范围 0-33）、Overlay 字段能力注册表（`overlay.OverlayFieldCapabilities`）与按查询实际生成的动态 `dependencySet` planner、媒体 `If-Range`、目标化单媒体按需内容确认（`POST .../media/{mediaId}/verification-jobs` 只强制目标媒体重新哈希，不再触发所属 Source 的整体 `verify`；EV-32 补齐了冻结 MediaID/ObservationFingerprint 在执行阶段的真实一致性验证，EV-33 独立复核确认该验证准确无新缺口，但该轮复核未覆盖 publication 维度——EV-34 发现 `VerificationTarget` 当时未冻结请求所依据的 `queryPublicationId`，HTTP 层用请求 publication 解析媒体身份、却用固定的 active-bound 查询读取 observation，执行阶段同样读取执行时刻的 active publication 而非请求时冻结的那个，已改为显式冻结 `queryPublicationId`、限定为只对当前 active publication 生效、执行阶段读取该冻结 publication、幂等键纳入 `queryPublicationId`、前置身份不匹配统一为不可重试的 `VERIFICATION_TARGET_MISMATCH`）、媒体/DerivedAsset 的 `queryPublicationId` 查询快照绑定读取（current/snapshot 两种模式 + 专用 publication 读取租约）、独立 `media.derive` capability、DerivedAsset 公共契约与受限 JPEG 缩略图端到端路径（EV-32 把异步输入解析改为跨 revision 内容寻址并补齐 `media.BlobReadLease` 保护，修正了 Resolver 失败错误码被统一改写的问题；EV-33 发现该保护仍只依赖固定 TTL、且 DerivedAsset Job 默认 `MaxRetries=0` 导致退避重试从未生效，已改为 GC 额外参照非终态/退避等待中的 Job 状态并补齐默认重试策略）、v9→v10 查询快照列的启动回填（EV-32 首次实现；EV-33 发现合并到既有 Job 时会在其真正 `completed` 之前提前写入完成标记，已改为驱动 Job 到真正终态后才标记）、多值查询投影 `FieldSeparator` 碰撞在权威输入边界的拒绝（EV-32）。ranking 具体权重数值、total 预算、cursor/publication 读取租约时长、发布/发现时间与 WorkOrigin 等过滤字段覆盖、AND/OR 子项 canonical 化、Progress 排序、百万/千万级正式查询语料、真实 Reference/Degradation Performance 和最终 API Freeze 审计均留待下一轮独立执行，不得据此声称阶段 4 API Freeze Gate 或真实规模压力测试已通过。

- 完成结构化过滤、FTS5 bigram/trigram、原文复核和搜索分页；**（已完成代码主线与合成 Correctness，含 Favorite/Hidden/Progress 过滤字段）**
- 冻结排序黄金语料、null 语义和游标签名；**（自然排序黄金语料与游标签名代码主线已完成；字段级 ranking（v2）结构已冻结，具体权重与 cursor 租约时长仍 PRE_FREEZE）**
- 完成有效字段投影、`query_publication_id`、独立 `overlay_projection_revision` 的构建/发布/租约，以及 Overlay 写后屏障和失败恢复；**（已完成，Overlay 字段能力注册表与按查询动态生成的 dependencySet planner已正式化，取代此前的全局静态 snapshot/live 分类）**
- 完成 CanonicalMedia/ContentBlob 的 Range、ETag、离线错误，并在阶段 3 已有 DerivedAsset Job 基础上冻结媒体读取与派生 API；**（媒体读取协议含 If-Range 已完成；媒体/DerivedAsset 读取已绑定 `queryPublicationId` 快照；DerivedAsset 已完成受限 JPEG 缩略图端到端公共契约与独立 `media.derive` capability，完整 ffmpeg/多格式转码延后）**
- 明确宽查询 total/highlight/ranking 后冻结 API Schema。**（total 协议版本字段、字段级 ranking v2 结构与通用 matches 高亮 DTO 已完成代码主线；具体预算与权重数值冻结留待下一轮压力测试）**

## 阶段 5：账户、安全和多客户端

- Personal 配对、Host/Origin/Fetch Metadata/CSRF、Session/WS 吊销；
- LAN Owner 初始化、Argon2id、本地账户、API Token 和 Library grant；
- available/effective capabilities 在 API、Web、CLI 全链一致；
- 分享 scope、过期和吊销；
- 威胁模型中的路径、恶意 metadata、恶意媒体和限速门禁。

## 阶段 6：Web/PWA

- 浏览、搜索、CanonicalWork、CanonicalCreator、CanonicalMedia 查看、Overlay、Job、规则和管理最小页面；
- headless 可访问组件 + 语义令牌；
- 键盘/焦点/ARIA、reduced motion、对比度和移动触控；
- 所有 UI 状态从 API/Schema/effective capabilities 产生；
- 普通浏览器完成所有业务闭环后，才开始壳集成。

## 阶段 7：平台与发行

- Windows 真实运行、升级、崩溃恢复、junction/reparse、CredentialStore 和签名；
- Linux 原生 ext4 核心门禁；macOS 和 Docker 按目标支持级别执行；
- SMB/NAS/UNC 独立门禁；
- Wails 与 Tauri 对照，决定是否将 Wails 从有条件接受提升；
- 程序/数据分离、备份恢复、版本升级、SBOM 和依赖安全检查。

## v1 必含与延期

| 必含 | 延期 |
| --- | --- |
| Go 模块化单体、SQLite、Web/PWA | 微服务、远程数据库、外部队列 |
| Source-derived/Canonical/Binding/Overlay | 自动导入其他产品数据 |
| staging revision、Job/Saga、FTS | 外部搜索服务 |
| 规则原语 + 受限 CEL | 任意脚本、WASM/进程插件 |
| Personal/LAN、capability | Remote/OIDC、多租户 |
| 只读媒体、Range、缩略图 | 原文件写入、容器格式深度解析 |
| Windows 正式支持；其他按门禁 | 原生移动客户端 |

## 不阻塞开工的不确定性

以下问题只要保留端口和版本字段，不阻塞阶段 0/垂直切片：最终前端框架、Wails/Tauri、macOS 是否列入 v1、精确 total 的 UI、语言读音模块、插件运行时、压缩包/PDF 支持。

以下问题会阻塞相关冻结：Binding 唯一约束阻塞领域 Schema；`query_publication_id` 对内部合法 revision 元组的绑定、Overlay 写后屏障的物理实现门禁阻塞查询 Schema；游标租约和 total/highlight 阻塞完整 API；平台文件身份和只读句柄复核阻塞正式平台发布。这些不阻塞按正式基础模型启动 Walking Skeleton。

完整门禁见 [测试与发布门禁](02-测试与发布门禁.md)。
