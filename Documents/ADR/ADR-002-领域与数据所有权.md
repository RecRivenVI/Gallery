# ADR-002 领域与数据所有权

- 状态：接受
- 日期：2026-07-16
- 规范：[领域模型与数据所有权](../规范/03-领域模型与数据所有权.md)

## 问题

扫描结果会随 Source、规则和文件变化而重建，但用户合并、覆盖、收藏和进度必须长期稳定。同时，逻辑媒体、字节内容和文件位置具有不同生命周期；如果压成一张“作品/文件”表，重扫、移动和去重会破坏用户状态。

## 决策

`catalog.db` 保存 SourceWork/SourceCreator/SourceMedia、ContentBlob/FileLocation 和可重建查询投影；`control.db` 保存 CanonicalWork/CanonicalCreator/CanonicalMedia、WorkBinding/CreatorBinding/MediaBinding、Overlay、账户和其他不可重建事实。Source-derived 实体通过稳定 Source 引用和持久 Binding 指向 Canonical 实体；`control.db` 不得保存 Catalog revision 内部 row ID。

CanonicalMedia 是 CanonicalWork 中的逻辑 occurrence；ContentBlob 是完整字节内容，v1 以算法版本 + 完整 SHA-256 确认；FileLocation 是可变位置。ContentBlob/FileLocation 随 Catalog revision 可重建，CanonicalMedia 的当前内容由 MediaBinding 和查询投影解析；固定内容版本在 control 中保存算法版本 + digest，而不是 Blob row ID。DerivedAsset 以 Blob 稳定引用、变换版本、参数和相关 Overlay 输入为键，其登记与缓存均可重建。

Provider 是轻量注册表；Source 与 Provider 多对多。CanonicalWork 可有多个 Origin；CanonicalWork 与 CanonicalCreator 通过带 role/ordinal 的关系多对多。

## 理由

- Catalog 可删后重扫，而用户事实不可恢复；两者必须有不同生命周期；
- 稳定 Binding 允许 Source 路径改变、来源消失和 Canonical 合并而不丢用户组织；
- 人工构造的采样碰撞证明快速签名不能确认内容；
- 复制、硬链接和同字节多 occurrence 证明 FileID、ContentBlob 和 CanonicalMedia 不能合并；
- 真实样本发现重复 ContentBlob 和多种 Origin/Creator 结构，见 [EV-03](../证据/验证记录.md#ev-03-媒体身份e1--e3e4)。

## 替代方案

- **所有状态放一个可重建 Catalog**：重扫会丢或覆盖用户事实。
- **增加第三个 Overlay 数据库**：增加跨库协调，没有独立生命周期收益；control 足以承载。
- **路径作为 CanonicalWork/CanonicalMedia 主键**：改名、移动和目录重排会制造新实体。
- **采样哈希作为 Blob 主键**：已被确定性碰撞否定。
- **FileID/inode 作为统一身份**：复制、跨卷和重挂载会改变，且不能表达逻辑 occurrence。
- **Provider 拥有平台业务对象和代码分支**：把规则差异固化到核心，破坏可配置边界。

## 影响

- 查询需要将 control Overlay 投影到 Catalog，但投影不是权威；
- 备份必须优先保护 control，Catalog/搜索/派生资源可重建；
- 重扫只能更新 Source-derived 行和 Binding 状态，不能删除 Canonical/Overlay；
- ContentBlob 去重只复用字节与派生计算，不自动合并 CanonicalWork/CanonicalMedia；
- **媒体“已发现”与“内容已确认”解耦（2026-07-19 修订，见下方演进说明）**：默认 `incremental` 扫描档案与显式 `verify` 档案下，新 ContentBlob 的首次完整 SHA-256 仍是该媒体进入 `content_verified`/建立 ContentBlob 记录的前置条件；但用户显式选择的 `index` 档案允许媒体以 `located_unverified` 状态先行发布，此时不建立 ContentBlob/FileLocation 记录，不得以任何快速指纹、路径摘要或占位值冒充 digest。`located_unverified` 与 `content_verified` 是发布出的合法查询状态，不是构建中的半发布 candidate；`verify` 档案或按需 Hash Job 完成后原地提升为 `content_verified`。超大文件或网络盘沿用原语义，可以延后发布但不能用快速指纹建立权威 Blob；
- DerivedAsset 数据库登记丢失后只能凭完整稳定 key 和受校验 manifest 接管缓存，不能按文件名猜测；
- Catalog 删除重建后必须以稳定 Source 引用和 Blob digest 恢复 Binding/固定内容，不能依赖旧 row ID；
- 最终数据库唯一约束必须等网络盘和文件身份门禁完成。

## 重新审议条件

实体边界属于产品不变量，仅在出现无法由 Binding/Origin/CanonicalMedia/ContentBlob/FileLocation 表达的真实生命周期时重开。哈希算法升级应增加 algorithm/version，不得退回路径或采样身份。SourceMedia 的观察证据（mtime、平台身份线索、容器签名）与已确认内容状态是本次修订新增的可调节字段，不构成需要重开本节的实体边界变化。

## 演进说明

- 2026-07-17：control.db 产品级备份/恢复落地（见 [验证记录 EV-19](../证据/验证记录.md)），作为本决策「control 最高备份优先级、Catalog 可重建」的实现基线，不改变实体边界或所有权矩阵。备份为 control.db 的 SQLite 一致性副本，附带 `manifestVersion=1` 的自描述 manifest（role、schemaVersion、migrationChecksum、checksum、安全范围）；恢复在下次启动、打开数据库前于 AppDirs 单写者锁下隔离迁移并原子替换，失败保留当前库，恢复后作废 Session/pairing 与非终态 Job。备份 manifest 版本与恢复应用模型的冻结阶段与重新审议门禁登记于 [v1 实施计划](../指南/01-v1实施计划.md) 的「暂定实装决策」表。此说明不新增独立 ADR。
- 2026-07-18：SourceWork 拆分/合并落地并执行阶段 1 Schema Freeze Gate（见 [验证记录 EV-20](../证据/验证记录.md)），阶段 1 结束。拆分/合并首先是 Source-derived 结构变化，不自动等同 CanonicalWork 拆分/合并；检测只用 ContentBlob digest 证据，无法安全自动处理时复用 Binding issue 阻塞 publication，人工决策以 control.db 的 pre-seed WorkBinding（继承写 inactive、排除写 manual_unbound）表达。撤回仅限尚未被扫描消费的 pre-seed 决策：清除本次 pre-seed Binding 并重开 issue；已消费时返回 `CONFLICT`，决策、issue 与 Binding 不部分修改，已生效结构变化需新的人工决策或未来补偿流程。决策与溯源持久化在 `source_structure_decisions`，Catalog 全量重建后可重放。此实现落在本决策「稳定 Binding、control 拥有人工决策、Catalog 可重建、不依赖 row ID、去重不合并 Canonical」的既有边界内，未改变实体边界或所有权矩阵，不新增独立 ADR。阶段 1 Schema Freeze Gate 把身份与唯一约束逐项分类登记在 control 迁移 `00016_schema_freeze_phase1` 的 `schema_freeze` 表：`(source_id, source_key) WHERE status='active'` active Binding 唯一、`(work_id, ordinal)` 唯一、CanonicalWork 仅以持久 ID 标识、同 Blob 多 occurrence、结构决策 fingerprint 唯一等冻结为兼容承诺（FROZEN），破坏前须新增或修订 ADR；orphan 阈值、external ID 冲突策略、RuleVersion 身份命名空间、WorkOrigin 模型、FileLocation 唯一约束等保持 COMPATIBILITY_BASELINE/PRE_FREEZE/DEFERRED，仍可经 forward-only migration 演进。本 ADR「最终数据库唯一约束必须等网络盘和文件身份门禁完成」的影响项据此细化为：Binding/CanonicalWork/CanonicalMedia 的核心身份约束已冻结，FileLocation 在 SMB/inode/无 FileID 下的唯一约束仍待文件身份门禁。
- 2026-07-19：真实 SSD/HDD 大数据集结构抽样、只读清单和有界样本验收（全量扫描未完成，正式全量性能 Gate 仍未通过）发现默认全量完整哈希扫描在真实规模（真实 HDD 上约 8 items/sec，20 万+ 文件级图库预估需约 22 小时）下不适合作为日常扫描路径，用户决定终止本轮全量完整哈希验收并将扫描拆分为三种正式档案：`index`（发现并发布 `located_unverified` 媒体，不建立 ContentBlob）、`incremental`（默认；按 Source、规范化相对路径、大小、mtime 组合证据判断是否复用既往 `content_verified` 摘要，仅对新增或疑似变化媒体建立 Hash Job）、`verify`（忽略既往观察，对本次扫描到的媒体强制重新完整哈希，定位为显式、低频的完整性校验）。`catalog.db` migration `00008_content_verification.sql` 在 `source_media` 新增 `mtime_ns`、`platform_identity_kind`、`platform_identity_value`、`container_signature`、`content_verification_state`、`last_confirmed_algorithm`、`last_confirmed_digest`、`last_confirmed_at`；`content_blobs`/`file_locations` 结构不变，只在媒体达到 `content_verified` 时才为其写入对应行，未确认媒体不产生任何 ContentBlob/FileLocation 记录。`media_projections` 新增独立的 `content_verification_state`/`verified_at`，与只表达位置可用性的既有 `location_status`（`present`/`offline`/`missing`/`inaccessible`）语义正交（首次实现曾借用 `location_status='located_unverified'` 表达内容确认状态，2026-07-19 收尾修正为独立字段，见下）。本次修订不改变 ContentBlob 唯一以完整 digest 确认、FileID/mtime 仅作候选证据、Source 只读、Catalog publication 原子一致等既有不变量；`platform_identity_kind`/`platform_identity_value` 本轮暂未接入真实 Windows FileID/`dev+inode`（无对应平台适配端口），只使用规范化相对路径 + 大小 + mtime 的组合证据，真实平台文件身份仍是文件身份门禁的待办项。详见 [扫描、Catalog 与任务](../规范/04-扫描-Catalog与任务.md#扫描档案与内容确认状态) 与 [文件系统与媒体处理](../规范/08-文件系统与媒体处理.md#扫描档案与内容确认)。
- 2026-07-19（收尾修正，见 [验证记录 EV-26](../证据/验证记录.md)）：上一条演进说明落地的首个实现在 `media_projections` 上把 `location_status` 借值为 `located_unverified`，与本决策「媒体已发现与内容已确认解耦」的既有边界不一致（位置可用性与内容确认状态本应正交）。forward-only migration `00009_media_projection_verification.sql` 为 `media_projections` 增加独立的 `content_verification_state`、`verified_at` 列并回填历史数据，`location_status` 恢复为只表达位置可用性；媒体内容端点相应新增专用 `CONTENT_NOT_VERIFIED` 错误码，不再复用 Source 离线的 `MEDIA_OFFLINE`。同一轮还修正了未显式指定 `scanProfile` 时的默认选择逻辑（按 Source 是否已有 publication 自动选 `index`/`incremental`，不再固定为 `incremental`）、非法档案值的严格校验、`incremental` 复用既往摘要前的 TOCTOU 复核缺口，以及 `last_confirmed_at`/`verified_at` 只应在真正完成完整哈希时推进的语义。这些都是对本决策既有边界的正确性收口，不改变实体边界、所有权矩阵或已冻结的领域身份约束，不新增独立 ADR。
- 2026-07-20（阶段 3 最终 Correctness 收口，见 [验证记录 EV-27](../证据/验证记录.md)）：上一条演进说明的默认选择逻辑只看 Catalog 当前是否有 publication，未覆盖「Catalog 被删除重建、但 control.db 仍保留该 Source 的 Binding/结构决策历史」这一真实边界情形——此时若仍按"当前无 publication"判定为全新 Source 并自动选择 `index`，会让本次扫描不产生任何 ContentBlob digest，悄悄绕过阶段 1 依赖完整哈希证据的 SourceWork 拆分/合并结构审查。新增 `Resources.SourceHasDurableHistory` 综合 `work_bindings`/`media_bindings`/`creator_bindings`/Binding issue/SourceWork 结构决策判断持久领域历史，默认选择改为「无 publication 且无持久领域历史 → index；有 publication，或无 publication 但有持久领域历史 → incremental」，显式 `index` 相应只在无 publication 且无持久领域历史时被接受。同一轮还修正了 `index` 档案在最终定位观察后遗漏同步 `ExpectedModTimeNanos`、可能把 discovery 时的旧 mtime 与最终定位的新 size 拼接成混合 observation 的缺陷，以及 `internal/recovery`/`internal/overlay` 中业务 Reconcile 与中央 Recovery Service 对同一 queued Job 的竞争领取窗口。这些都是对本决策与阶段 1 结构审查既有边界的正确性收口，不改变实体边界、所有权矩阵或已冻结的领域身份约束，不新增独立 ADR；`catalog_revisions.job_id` 当前仍是跨全部历史行的扁平 `UNIQUE` 约束这一独立发现（见 EV-27）尚未处理，留待后续评估。
